# Everything Claude Code (ECC) — エージェントインストラクション

これは、60個の特化型エージェント、231個のスキル、75個のコマンド、およびソフトウェア開発のための自動化されたフックワークフローを提供する**本番環境対応のAIコーディングプラグイン**である。

**バージョン:** 2.0.0-rc.1

## コア原則 (Core Principles)

1. **エージェントファースト (Agent-First)** — ドメインのタスクは特化型エージェントに委譲する
2. **テスト駆動 (Test-Driven)** — 実装の前にテストを記述する。80%以上のカバレッジが必須である
3. **セキュリティファースト (Security-First)** — セキュリティに決して妥協しない。すべての入力を検証する
4. **不変性 (Immutability)** — 常に新しいオブジェクトを作成し、既存のオブジェクトを絶対に変更しない
5. **実行前の計画 (Plan Before Execute)** — コードを書く前に複雑な機能の計画を立てる

## 利用可能なエージェント (Available Agents)

| Agent | 目的 (Purpose) | 使用するタイミング (When to Use) |
|-------|---------|-------------|
| planner | 実装の計画 | 複雑な機能、リファクタリング |
| architect | システム設計とスケーラビリティ | アーキテクチャの決定 |
| tdd-guide | テスト駆動開発 | 新機能、バグ修正 |
| code-reviewer | コードの品質と保守性 | コードの記述/変更後 |
| security-reviewer | 脆弱性の検出 | コミットの前、機密性の高いコード |
| build-error-resolver | ビルド/型エラーの修正 | ビルド失敗時 |
| e2e-runner | エンドツーエンドのPlaywrightテスト | 重要なユーザーフロー |
| refactor-cleaner | デッドコードのクリーンアップ | コードの保守 |
| doc-updater | ドキュメントとコードマップ | ドキュメントの更新時 |
| cpp-reviewer | C/C++コードレビュー | CおよびC++プロジェクト |
| cpp-build-resolver | C/C++ビルドエラー | CおよびC++のビルド失敗時 |
| fsharp-reviewer | F#関数型コードレビュー | F#プロジェクト |
| docs-lookup | Context7経由でのドキュメント検索 | API/ドキュメントの質問 |
| go-reviewer | Goコードレビュー | Goプロジェクト |
| go-build-resolver | Goビルドエラー | Goのビルド失敗時 |
| kotlin-reviewer | Kotlinコードレビュー | Kotlin/Android/KMPプロジェクト |
| kotlin-build-resolver | Kotlin/Gradleビルドエラー | Kotlinのビルド失敗時 |
| database-reviewer | PostgreSQL/Supabaseスペシャリスト | スキーマ設計、クエリ最適化 |
| python-reviewer | Pythonコードレビュー | Pythonプロジェクト |
| django-reviewer | Djangoコードレビュー | Djangoアプリ、DRF API、ORM、マイグレーション |
| django-build-resolver | Djangoのビルド、マイグレーション、およびセットアップのエラー | Djangoの起動、依存関係、マイグレーション、collectstaticの失敗時 |
| java-reviewer | JavaおよびSpring Bootコードレビュー | Java/Spring Bootプロジェクト |
| java-build-resolver | Java/Maven/Gradleビルドエラー | Javaのビルド失敗時 |
| loop-operator | 自律型ループ実行 | ループの安全な実行、ストールの監視、介入 |
| harness-optimizer | ハーネス設定のチューニング | 信頼性、コスト、スループット |
| rust-reviewer | Rustコードレビュー | Rustプロジェクト |
| rust-build-resolver | Rustビルドエラー | Rustのビルド失敗時 |
| pytorch-build-resolver | PyTorchランタイム/CUDA/トレーニングエラー | PyTorchのビルド/トレーニング失敗時 |
| mle-reviewer | 本番MLパイプラインレビュー | MLパイプライン、評価、サービング、モニタリング、ロールバック |
| typescript-reviewer | TypeScript/JavaScriptコードレビュー | TypeScript/JavaScriptプロジェクト |

## エージェントのオーケストレーション (Agent Orchestration)

ユーザーのプロンプトを待たずに、エージェントを自発的に使用すること:
- 複雑な機能の要求 → **planner**
- コードを記述/変更した直後 → **code-reviewer**
- バグ修正または新機能 → **tdd-guide**
- アーキテクチャの決定 → **architect**
- セキュリティに敏感なコード → **security-reviewer**
- 自律型ループ / ループの監視 → **loop-operator**
- ハーネス設定の信頼性とコスト → **harness-optimizer**

独立した操作には並行実行を利用すること — 複数のエージェントを同時に起動する。

## セキュリティガイドライン (Security Guidelines)

**いかなるコミットの前にも (Before ANY commit):**
- シークレット（APIキー、パスワード、トークン）をハードコードしないこと
- すべてのユーザー入力が検証されていること
- SQLインジェクションの防止（パラメータ化されたクエリ）
- XSSの防止（サニタイズされたHTML）
- CSRF保護が有効になっていること
- 認証/認可が検証されていること
- すべてのエンドポイントでのレート制限
- エラーメッセージが機密データを漏洩しないこと

**シークレットの管理:** シークレットは絶対にハードコードしない。環境変数またはシークレットマネージャーを使用すること。起動時に必須のシークレットを検証する。露出したシークレットは直ちにローテーションする。

**セキュリティの問題が見つかった場合:** 停止する (STOP) → security-reviewerエージェントを使用する → 致命的な (CRITICAL) 問題を修正する → 露出したシークレットをローテーションする → コードベースで同様の問題をレビューする。

## コーディングスタイル (Coding Style)

**不変性 (CRITICAL):** 常に新しいオブジェクトを作成し、決して変更(mutate)しないこと。変更を適用した新しいコピーを返すこと。

**ファイルの構成:** 少数の大きなファイルよりも、多数の小さなファイルを好むこと。通常200〜400行、最大800行。タイプごとではなく、機能/ドメインごとに整理すること。高凝集度、低結合度。

**エラー処理:** あらゆるレベルでエラーを処理すること。UIコードではユーザーフレンドリーなメッセージを提供すること。サーバーサイドでは詳細なコンテキストを記録すること。エラーを暗黙のうちに握りつぶさないこと。

**入力検証:** システムの境界ですべてのユーザー入力を検証すること。スキーマベースの検証を使用すること。明確なメッセージと共にフェイルファスト (Fail fast) すること。外部データを絶対に信用しないこと。

**コード品質チェックリスト:**
- 関数は小さく（50行未満）、ファイルは焦点が絞られていること（800行未満）
- 深いネスト（4レベル以上）がないこと
- 適切なエラー処理があり、ハードコードされた値がないこと
- 読みやすく、名前が適切に付けられた識別子

## テスト要件 (Testing Requirements)

**最小カバレッジ: 80%**

テストタイプ（すべて必須）:
1. **ユニットテスト** — 個別の関数、ユーティリティ、コンポーネント
2. **統合テスト** — APIエンドポイント、データベース操作
3. **E2Eテスト** — 重要なユーザーフロー

**TDDワークフロー（必須）:**
1. 最初にテストを書く (RED) — テストは失敗(FAIL)すること
2. 最小限の実装を書く (GREEN) — テストは成功(PASS)すること
3. リファクタリング (IMPROVE) — カバレッジが80%以上であることを検証する

失敗のトラブルシューティング: テストの分離を確認する → モックを検証する → 実装を修正する（テストが間違っていない限り、テスト自体は修正しない）。

## 開発ワークフロー (Development Workflow)

1. **計画 (Plan)** — plannerエージェントを使用し、依存関係とリスクを特定し、フェーズに分割する
2. **TDD** — tdd-guideエージェントを使用し、最初にテストを書き、実装し、リファクタリングを行う
3. **レビュー (Review)** — 直ちにcode-reviewerエージェントを使用し、CRITICAL/HIGHな問題に対処する
4. **適切な場所での知識の獲得**
   - 個人のデバッグメモ、設定、一時的なコンテキスト → 自動メモリ
   - チーム/プロジェクトの知識（アーキテクチャの決定、APIの変更、ランブック） → プロジェクトの既存のドキュメント構造
   - 現在のタスクがすでに関連するドキュメントやコードコメントを生成している場合、同じ情報を別の場所に複製しないこと
   - 明確なプロジェクトドキュメントの場所がない場合は、新しいトップレベルのファイルを作成する前に尋ねること
5. **コミット (Commit)** — Conventional Commitsのフォーマット、包括的なプルリクエストの要約

## ワークフローサーフェスポリシー (Workflow Surface Policy)

- `skills/` は正規のワークフローサーフェスである。
- 新しいワークフローの貢献は、まず `skills/` に配置されるべきである。
- `commands/` は互換性のためのレガシーなスラッシュエントリのサーフェスであり、移行やクロスハーネスのパリティのためにシムがまだ必要な場合にのみ追加または更新されるべきである。

## Gitワークフロー (Git Workflow)

**コミットフォーマット:** `<type>: <description>` — Typeには以下が含まれる: feat, fix, refactor, docs, test, chore, perf, ci

**プルリクエストワークフロー:** コミット履歴全体を分析する → 包括的な要約をドラフトする → テスト計画を含める → `-u` フラグを付けてプッシュする。

## アーキテクチャパターン (Architecture Patterns)

**APIレスポンスフォーマット:** 成功インジケーター、データペイロード、エラーメッセージ、およびページネーションメタデータを持つ一貫したエンベロープ。

**リポジトリパターン:** 標準インターフェース（findAll, findById, create, update, delete）の背後にデータアクセスをカプセル化する。ビジネスロジックはストレージメカニズムではなく、抽象インターフェースに依存する。

**スケルトンプロジェクト:** 実績のあるテンプレートを検索し、並行エージェント（セキュリティ、拡張性、関連性）で評価し、最適な一致を複製し、実績のある構造内でイテレーションを行う。

## パフォーマンス (Performance)

**コンテキスト管理:** 大規模なリファクタリングや複数ファイルの機能において、コンテキストウィンドウの最後の20%を使用しないこと。重要度の低いタスク（単一の編集、ドキュメント、単純な修正）では、より高い使用率が許容される。

**ビルドのトラブルシューティング:** build-error-resolverエージェントを使用する → エラーを分析する → 段階的に修正する → 各修正後に検証する。

## プロジェクト構造 (Project Structure)

```
agents/          — 60個の特化型サブエージェント
skills/          — 231個のワークフロースキルとドメイン知識
commands/        — 75個のスラッシュコマンド
hooks/           — トリガーベースの自動化
rules/           — 常に従うべきガイドライン（共通 + 言語別）
scripts/         — クロスプラットフォームのNode.jsユーティリティ
mcp-configs/     — 14個のMCPサーバー設定
tests/           — テストスイート
```

`commands/` は互換性のためにリポジトリに残されているが、長期的な方向性はスキルファーストである。

## 成功の指標 (Success Metrics)

- すべてのテストが80%以上のカバレッジで合格すること
- セキュリティの脆弱性がないこと
- コードが読みやすく、保守可能であること
- パフォーマンスが許容範囲であること
- ユーザーの要件が満たされていること
