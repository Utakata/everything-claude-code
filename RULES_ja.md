# Rules

## Must Always
- ドメインのタスクは特化型エージェントに委任する。
- 実装前にテストを記述し、クリティカルパスを検証する。
- 入力を検証し、セキュリティチェックをそのまま維持する。
- 共有状態を変更（ミューテーション）するのではなく、イミュータブルな更新を優先する。
- 新しいパターンを発明する前に、確立されたリポジトリのパターンに従う。
- コントリビューションは焦点を絞り、レビュー可能にし、適切に説明された状態に保つ。

## Must Never
- APIキー、トークン、シークレット、絶対パス/システムファイルパスなどの機密データを推論出力に含めない。
- テストされていない変更を送信しない。
- セキュリティチェックや検証フックをバイパスしない。
- 明確な理由なしに既存の機能を複製しない。
- 関連するテストスイートをチェックせずにコードをリリースしない。

## Agent Format
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含むYAMLフロントマターを含める。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させる必要がある。
- `description` は、そのエージェントをいつ呼び出すべきかを明確に伝える必要がある。

## Skill Format
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を含むYAMLフロントマターを含める。
- ファーストパーティのスキルには `origin: ECC` を使用し、インポートされた/コミュニティのスキルには `origin: community` を使用する。
- スキルの本文には、実践的なガイダンス、テスト済みの例、および明確な「When to Use」セクションを含める必要がある。

## Hook Format
- フックは、マッチャー駆動のJSON登録とシェルまたはNodeのエントリポイントを使用する。
- マッチャーは、広範囲のすべてをキャッチするものではなく、具体的なものにする必要がある。
- ブロック動作が意図的である場合にのみ `1` で終了し、それ以外の場合は `0` で終了する。
- エラーおよび情報メッセージは、実行可能（アクション可能）なものにする必要がある。

## Commit Style
- `feat(skills):`、`fix(hooks):`、または `docs:` のようなConventional Commitsを使用する。
- 変更はモジュール化し、PRのサマリーでユーザーへの影響を説明する。
