# ルール

## 必須事項 (Must Always)
- ドメイン固有のタスクは特化したエージェントに委譲すること。
- 実装前にテストを記述し、重要な経路を検証すること。
- 入力を検証し、セキュリティチェックを維持すること。
- 共有状態のミューテーションよりも、イミュータブルな更新を優先すること。
- 新しいパターンを発明する前に、リポジトリで確立されたパターンに従うこと。
- 変更内容は焦点を絞り、レビュー可能で、適切に説明された状態に保つこと。

## 禁止事項 (Must Never)
- APIキー、トークン、シークレット、絶対パスやシステムファイルパスなどの機密データを出力に含めること。
- 未テストの変更を送信すること。
- セキュリティチェックや検証フックを回避すること。
- 明確な理由なく既存の機能を複製すること。
- 関連するテストスイートをチェックせずにコードを提出すること。

## エージェントのフォーマット
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含む YAML のフロントマターを含める。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させること。
- `description` には、エージェントを呼び出すべきタイミングを明確に伝えること。

## スキルのフォーマット
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を含む YAML のフロントマターを含める。
- ファーストパーティのスキルには `origin: ECC` を、インポートされたスキルやコミュニティのスキルには `origin: community` を使用すること。
- スキルの本文には、実践的なガイダンス、テスト済みの例、および明確な「When to Use (使用するタイミング)」セクションを含めること。

## フックのフォーマット
- フックは、マッチャー駆動の JSON 登録と、シェルまたは Node のエントリーポイントを使用する。
- マッチャーは、広範囲をカバーするものではなく、具体的なものを指定すること。
- ブロッキング動作を意図する場合のみ `1` で終了し、それ以外の場合は `0` で終了すること。
- エラーおよび情報メッセージは、アクション可能な内容であること。

## コミットスタイル
- `feat(skills):`、`fix(hooks):`、`docs:` などの Conventional Commits を使用すること。
- 変更はモジュール単位に保ち、PR の概要でユーザーに与える影響を説明すること。