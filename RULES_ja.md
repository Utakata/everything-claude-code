# ルール

## Must Always（常に守るべきこと）
- 特定のドメインタスクは専門のエージェントに委譲すること。
- 実装前にテストを記述し、重要な処理経路（critical paths）を検証すること。
- 入力の検証を行い、セキュリティチェックを維持すること。
- 共有状態を直接変更するのではなく、イミュータブルな更新を優先すること。
- 新しいパターンを考案する前に、リポジトリ内で確立された既存のパターンに従うこと。
- コントリビューションはスコープを明確にし、レビュー可能で、適切に説明された状態を保つこと。

## Must Never（決してしてはならないこと）
- APIキー、トークン、シークレット、絶対パス/システムパスなどの機密データを出力に含めないこと。
- テストされていない変更をコミットしないこと。
- セキュリティチェックや検証フックをバイパスしないこと。
- 明確な理由なく既存の機能を重複させないこと。
- 関連するテストスイートの確認を行わずにコードをリリースしないこと。

## エージェントのフォーマット
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含むYAMLフロントマターを含めること。
- ファイル名は小文字のハイフン区切りとし、エージェント名と完全に一致させること。
- `description` には、エージェントを呼び出すべき状況を明確に記載すること。

## スキルのフォーマット
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を含むYAMLフロントマターを含めること。
- ファーストパーティのスキルには `origin: ECC` を、インポートされたスキルやコミュニティのスキルには `origin: community` を使用すること。
- スキルの本文には、実践的なガイダンス、テスト済みの例、および明確な「When to Use（いつ使用するか）」のセクションを含めること。

## フックのフォーマット
- フックは、マッチャー駆動型のJSON登録とシェルまたはNodeエントリポイントを使用する。
- マッチャーは、広範なキャッチオール（catch-all）ではなく、具体的な条件を指定すること。
- ブロック動作が意図的な場合のみ `exit 1` とし、それ以外は `exit 0` とすること。
- エラーおよび情報メッセージは、ユーザーが次にとるべき行動が明確な（actionable）ものとすること。

## コミットスタイル
- `feat(skills):`、`fix(hooks):`、`docs:` などのConventional Commitsに従うこと。
- 変更はモジュール単位に留め、ユーザーへの影響についてはプルリクエストの概要で説明すること。
