# Rules

## Must Always
- ドメインタスクには専門のエージェントに委譲すること。
- 実装前にテストを記述し、クリティカルパスを検証すること。
- 入力を検証し、セキュリティチェックを無傷のまま保つこと。
- 共有された状態をミューテート（変更）するよりも、イミュータブルな更新を優先すること。
- 新しいパターンを発明する前に、確立されたリポジトリパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、適切に説明された状態を保つこと。

## Must Never
- APIキー、トークン、シークレット、絶対パス/システムファイルパスなどの機密データを出力に含めること。
- テストされていない変更を提出すること。
- セキュリティチェックや検証フックをバイパスすること。
- 明確な理由なしに既存の機能を複製すること。
- 関連するテストスイートを確認せずにコードをシップすること。

## Agent Format
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含むYAMLフロントマターを含める。
- ファイル名はハイフン区切りの小文字であり、エージェント名と一致させる必要がある。
- description（説明）は、そのエージェントをいつ呼び出すべきかを明確に伝えるものでなければならない。

## Skill Format
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を含むYAMLフロントマターを含める。
- ファーストパーティスキルには `origin: ECC` を使用し、インポート/コミュニティスキルには `origin: community` を使用する。
- スキルの本体には、実践的なガイダンス、テストされた例、および明確な "When to Use" セクションを含めるべきである。

## Hook Format
- フックはマッチャー駆動型のJSON登録を使用し、シェルまたはNodeのエントリーポイントを持つ。
- マッチャーは、広範囲を対象とするキャッチオールではなく、具体的なものにすべきである。
- ブロッキング動作が意図的な場合にのみ `1` で終了し、それ以外の場合は `0` で終了する。
- エラーおよび情報メッセージは、アクション可能なものにするべきである。

## Commit Style
- `feat(skills):`、`fix(hooks):`、`docs:` などのconventional commits（規約に基づいたコミット）を使用する。
- 変更はモジュール化を保ち、PRのサマリーでユーザーに与える影響を説明する。
