# ルール

## 常に遵守すべきこと
- ドメイン固有のタスクは専門のエージェントに委譲すること。
- 実装前にテストを記述し、クリティカルパスを検証すること。
- 入力を検証し、セキュリティチェックを機能させたままにすること。
- 共有状態を直接変更（mutate）するのではなく、イミュータブルな更新を優先すること。
- 新しいパターンを発明する前に、リポジトリで確立されたパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、適切に説明された状態を保つこと。

## 禁止事項
- APIキー、トークン、機密情報（シークレット）、絶対パスやシステムのファイルパスなどの機密データを出力に含めないこと。
- テストされていない変更を提出（submit）しないこと。
- セキュリティチェックや検証フックをバイパスしないこと。
- 明確な理由なく既存の機能を重複させないこと。
- 関連するテストスイートを確認せずにコードをリリース（ship）しないこと。

## エージェントのフォーマット
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を指定した YAML フロントマターを含める。
- ファイル名は小文字とハイフンを使用し、エージェント名と一致させる。
- `description` には、そのエージェントを呼び出すべき状況を明確に記述する。

## スキルのフォーマット
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を指定した YAML フロントマターを含める。
- ファーストパーティのスキルには `origin: ECC` を使用し、インポートされた、あるいはコミュニティ提供のスキルには `origin: community` を使用する。
- スキルの本文には、実践的なガイダンス、テスト済みの例、そして明確な「When to Use（いつ使用するか）」セクションを含める。

## フックのフォーマット
- フックは、マッチャー駆動の JSON 登録と、シェルまたは Node のエントリポイントを使用する。
- マッチャーは、広範な条件（catch-alls）ではなく、具体的に指定する。
- ブロックする動作が意図的である場合のみ終了コード `1` を使用し、それ以外の場合は `0` を使用する。
- エラーや情報のメッセージは、次に取るべき行動が明確にわかるもの（actionable）にする。

## コミットスタイル
- `feat(skills):`、`fix(hooks):`、`docs:` などの Conventional Commits（規約に基づくコミット）を使用する。
- 変更はモジュール化された状態を保ち、ユーザーへの影響をプルリクエストのサマリーで説明する。
