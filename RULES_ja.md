# ルール

## 必ず行うこと
- ドメインタスクは専門のエージェント（Agent）に委譲すること。
- 実装前にテストを記述し、クリティカルパスを検証すること。
- 入力を検証し、セキュリティチェックをそのまま維持すること。
- 共有状態を直接変更（ミューテーション）するのではなく、イミュータブル（不変）な更新を優先すること。
- 新しいパターンを発明する前に、リポジトリで確立されたパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、分かりやすい説明を付けること。

## 決して行わないこと
- APIキー、トークン、シークレットなどの機密データ、または絶対パスやシステムパスを出力に含めること。
- テストされていない変更を送信すること。
- セキュリティチェックやバリデーションのフック（Hook）をバイパスすること。
- 明確な理由なく既存の機能を複製すること。
- 関連するテストスイートを確認せずにコードを出荷すること。

## エージェントフォーマット
- エージェント（Agent）は `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含む YAML フロントマター（Frontmatter）を含めること。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させること。
- description の値は、いつエージェントを呼び出すべきかを明確に伝えること。

## スキルフォーマット
- スキル（Skill）は `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を含む YAML フロントマターを含めること。
- ファーストパーティスキルには `origin: ECC` を、インポートされた/コミュニティのスキルには `origin: community` を使用すること。
- スキルの本文には、実践的なガイダンス、テスト済みの例、そして明確な「When to Use（いつ使用すべきか）」セクションを含めること。

## フックフォーマット
- フック（Hook）は、マッチャー駆動の JSON 登録と、シェルまたは Node のエントリーポイントを使用する。
- マッチャーは、広範なキャッチオールではなく、具体的なものにすること。
- ブロッキング動作が意図的な場合のみ `1` で終了し、それ以外の場合は `0` で終了すること。
- エラーメッセージや情報メッセージは、アクション可能な（ユーザーが対応できる）ものにすること。

## コミットスタイル
- `feat(skills):`、`fix(hooks):`、`docs:` のような Conventional Commits を使用すること。
- 変更はモジュール単位に保ち、ユーザーに与える影響をプルリクエストの概要で説明すること。
