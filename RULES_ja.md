# Rules

## Must Always
- ドメインタスクは専門のエージェント（Agent）に委譲すること。
- 実装前にテストを記述し、クリティカルパスを検証すること。
- 入力を検証し、セキュリティチェックを維持すること。
- 共有状態を直接変更するのではなく、不変（イミュータブル）な更新を優先すること。
- 新しいパターンを発明する前に、確立されたリポジトリのパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、説明が十分に記述された状態を保つこと。

## Must Never
- 出力にAPIキー、トークン、シークレット、絶対パス/システムファイルパスなどの機密データを含めないこと。
- テストされていない変更をコミット（Commit）しないこと。
- セキュリティチェックや検証フック（Hook）をバイパスしないこと。
- 明確な理由なく既存の機能を複製しないこと。
- 関連するテストスイート（カバレッジ（Coverage））を確認せずにコードを出荷しないこと。

## Agent Format
- エージェント（Agent）は `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含むYAMLフロントマター（Frontmatter）を含めること。
- ファイル名はハイフン区切りの小文字とし、エージェント（Agent）名と一致させること。
- description には、そのエージェント（Agent）をいつ呼び出すべきかを明確に伝達する内容を記述すること。

## Skill Format
- スキル（Skill）は `skills/<name>/SKILL.md` に配置する。
- 各スキル（Skill）には、`name`、`description`、`origin` を含むYAMLフロントマター（Frontmatter）を含めること。
- ファーストパーティのスキル（Skill）には `origin: ECC` を使用し、インポート/コミュニティのスキル（Skill）には `origin: community` を使用すること。
- スキル（Skill）の本文には、実践的なガイダンス、テスト済みの例、および明確な「いつ使用するか（When to Use）」セクションを含めること。

## Hook Format
- フック（Hook）は、マッチャー駆動型のJSON登録と、シェルまたはNodeのエンドポイント（Endpoint）を使用する。
- マッチャーは、広範なキャッチオールではなく、具体的であるべきである。
- ブロックする動作が意図的な場合のみ終了コード `1` を返し、それ以外の場合は `0` を返すこと。
- エラーおよび情報メッセージは、アクション可能なものであるべきである。

## Commit Style
- `feat(skills):`、`fix(hooks):`、`docs:` などのConventional Commitsを使用すること。
- 変更はモジュール化を保ち、プルリクエスト（Pull request）の要約でユーザー向けの影響を説明すること。
