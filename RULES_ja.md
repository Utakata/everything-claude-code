# Rules

## Must Always
- ドメインのタスクは専門的なエージェントに委譲すること。
- 実装の前にテストを記述し、クリティカルパスを検証すること。
- 入力を検証し、セキュリティチェックをそのまま維持すること。
- 共有状態を変更するよりも、イミュータブルな更新を優先すること。
- 新しいものを発明する前に、リポジトリの既存のパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、適切に説明された状態を保つこと。

## Must Never
- 出力にAPIキー、トークン、シークレット、または絶対/システムのファイルパスなどの機密データを含めないこと。
- テストされていない変更を送信しないこと。
- セキュリティチェックや検証フックをバイパスしないこと。
- 明確な理由なく既存の機能を複製しないこと。
- 関連するテストスイートを確認せずにコードを出荷しないこと。

## Agent Format
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、および `model` を含む YAML フロントマターを含めること。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させること。
- description には、エージェントをいつ呼び出すべきかを明確に記述すること。

## Skill Format
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、および `origin` を含む YAML フロントマターを含めること。
- ファーストパーティのスキルには `origin: ECC` を使用し、インポートされた/コミュニティのスキルには `origin: community` を使用すること。
- スキルの本文には、実践的なガイダンス、テスト済みの例、および明確な「いつ使用するか」のセクションを含めること。

## Hook Format
- フックは、マッチャー駆動型の JSON 登録と、シェルまたは Node のエントリポイントを使用する。
- マッチャーは、広範なキャッチオールではなく、具体的なものにすること。
- ブロックする動作が意図的な場合にのみ `1` で終了し、それ以外の場合は `0` で終了すること。
- エラーメッセージと情報メッセージは、アクション可能なものにすること。

## Commit Style
- `feat(skills):`、`fix(hooks):`、または `docs:` などの Conventional Commits を使用すること。
- 変更はモジュール化し、PR のサマリーでユーザーへの影響を説明すること。
