# ルール

## 必須事項
- ドメインタスクは特化したエージェントに委譲すること。
- 実装前にテストを記述し、クリティカルパスを検証すること。
- 入力をバリデーションし、セキュリティチェックを維持すること。
- 共有状態のミューテーションよりも不変（immutable）な更新を優先すること。
- 新しいパターンを発明する前に、確立されたリポジトリのパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、適切に説明されている状態を保つこと。

## 禁止事項
- APIキー、トークン、シークレット、絶対パスやシステムのファイルパスなどの機密データを出力に含めないこと。
- テストされていない変更を提出しないこと。
- セキュリティチェックやバリデーションフックをバイパスしないこと。
- 明確な理由なしに既存の機能を複製しないこと。
- 関連するテストスイートを確認せずにコードを出荷しないこと。

## エージェントのフォーマット
- エージェントは `agents/*.md` に配置すること。
- 各ファイルには、`name`、`description`、`tools`、`model` を含むYAMLフロントマターを含めること。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させること。
- `description` は、エージェントを呼び出すべきタイミングを明確に伝えるものであること。

## スキルのフォーマット
- スキルは `skills/<name>/SKILL.md` に配置すること。
- 各スキルには、`name`、`description`、`origin` を含むYAMLフロントマターを含めること。
- ファーストパーティのスキルには `origin: ECC` を使用し、インポート/コミュニティのスキルには `origin: community` を使用すること。
- スキルの本文には、実践的なガイダンス、テスト済みの例、および明確な「When to Use」セクションを含めること。

## フックのフォーマット
- フックは、マッチャー駆動のJSON登録と、シェルまたはNodeのエンドポイントを使用すること。
- マッチャーは広範囲をカバーするキャッチオールではなく、具体的にすること。
- ブロッキング動作が意図的である場合にのみ `1` で終了（exit）し、それ以外の場合は `0` で終了すること。
- エラーおよび情報メッセージは、アクション可能（actionable）であること。

## コミットスタイル
- `feat(skills):`、`fix(hooks):`、`docs:` のようなコンベンショナルコミット（conventional commits）を使用すること。
- 変更はモジュール式に保ち、プルリクエストのサマリーでユーザーへの影響を説明すること。
