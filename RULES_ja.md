# Rules

## Must Always
- ドメインのタスクについては、専門のAgent（エージェント）に委譲すること。
- 実装前にテストを書き、クリティカルパスを検証すること。
- 入力を検証し、セキュリティチェックをそのまま維持すること。
- 共有状態を変更（mutate）するよりも、イミュータブル（不変）な更新を優先すること。
- 新しいパターンを発明する前に、リポジトリで確立されたベストプラクティスとパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、適切に説明された状態を保つこと。

## Must Never
- APIキー、トークン、シークレット、絶対パス/システムファイルパスなどの機密データを、出力に含めないこと。
- テストされていない変更を送信（Submit）しないこと。
- セキュリティチェックやバリデーションHook（フック）をバイパスしないこと。
- 明確な理由なく、既存の機能を複製しないこと。
- 関連するテストスイートのカバレッジ（Coverage）を確認せずにコードを出荷しないこと。

## Agent Format
- エージェント（Agent）は `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、および `model` を含むYAMLフロントマター（Frontmatter）を含める。
- ファイル名はハイフン区切りの小文字であり、エージェント名と一致させること。
- description（説明）は、そのエージェントをいつ呼び出すべきかを明確に伝えること。

## Skill Format
- スキル（Skill）は `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、および `origin` を含むYAMLフロントマターを含める。
- ファーストパーティスキルには `origin: ECC` を、インポートされた/コミュニティのスキルには `origin: community` を使用する。
- スキルの本文には、実践的なガイダンス、テスト済みの例、明確な「いつ使用するか（When to Use）」セクションを含める。

## Hook Format
- フック（Hook）はマッチャー駆動のJSON登録を使用し、シェルまたはNodeをエンドポイントとして使用する。
- マッチャーは、広範なキャッチオールではなく、特定の条件を指定すること。
- ブロッキング動作が意図的な場合のみ `1` を終了コード（Exit）とし、それ以外は `0` を終了コードとすること。
- エラーおよび情報メッセージは、アクション可能なものにすること。

## Commit Style
- `feat(skills):`、`fix(hooks):`、または `docs:` のようなConventional Commitsを使用すること。
- 変更はモジュール化を保ち、PRサマリーでユーザーへの影響を説明すること。
