# Rules

## Must Always
- ドメインのタスクには、特化したエージェントに委譲する。
- 実装前にテストを記述し、クリティカルパスを検証する。
- 入力を検証し、セキュリティチェックを維持する。
- 共有状態の変更よりも、不変な更新を優先する。
- 新しいパターンを発明する前に、確立されたリポジトリのパターンに従う。
- コミット（コントリビューション）は焦点が絞られ、レビュー可能で、適切に説明された状態を保つ。

## Must Never
- APIキー、トークン、シークレット、または絶対/システムのファイルパスなどの機密データを出力に含めない。
- テストされていない変更を送信（サブミット）しない。
- セキュリティチェックや検証フックをバイパスしない。
- 明確な理由なしに既存の機能を複製しない。
- 関連するテストスイートを確認せずにコードを出荷（シップ）しない。

## Agent Format
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、および `model` を含むYAMLフロントマターを含める。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させる必要がある。
- `description` には、エージェントが呼び出されるべきタイミングを明確に伝える必要がある。

## Skill Format
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、および `origin` を含むYAMLフロントマターを含める。
- ファーストパーティのスキルには `origin: ECC` を使用し、インポートされた/コミュニティのスキルには `origin: community` を使用する。
- スキルの本文には、実践的なガイダンス、テスト済みの例、および明確な「When to Use」セクションを含める必要がある。

## Hook Format
- フックは、マッチャー駆動のJSON登録と、シェルまたはNodeのエントリポイントを使用する。
- マッチャーは、広範なキャッチオールではなく、特定の条件を指定する必要がある。
- ブロッキング動作が意図的な場合のみ `1` で終了し、それ以外の場合は `0` で終了する。
- エラーおよび情報メッセージは、実行可能（アクショナブル）なものである必要がある。

## Commit Style
- `feat(skills):`、`fix(hooks):`、または `docs:` などのConventional Commitsを使用する。
- 変更はモジュール化し、PRサマリーでユーザーへの影響を説明する。
