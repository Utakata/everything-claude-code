# ルール

## 必ず行うこと
- ドメインタスクは専門のエージェントに委譲する。
- 実装前にテストを記述し、クリティカルパスを検証する。
- 入力を検証し、セキュリティチェックをそのまま維持する。
- 共有状態をミューテートするよりも、イミュータブルな更新を優先する。
- 新しいパターンを発明する前に、リポジトリで確立されたパターンに従う。
- コントリビューションは焦点を絞り、レビュー可能で、説明が十分に記述された状態を保つ。

## 絶対にしてはいけないこと
- APIキー、トークン、シークレット、絶対パスまたはシステムのファイルパスなどの機密データを出力に含めない。
- テストされていない変更をサブミットしない。
- セキュリティチェックやバリデーションフックをバイパスしない。
- 明確な理由なしに既存の機能を重複させない。
- 関連するテストスイートを確認せずにコードを出荷しない。

## エージェントのフォーマット
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含む YAMLフロントマターを含める。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させる。
- `description` には、エージェントをいつ呼び出すべきかを明確に伝える内容を記述する。

## スキルのフォーマット
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を含む YAMLフロントマターを含める。
- ファーストパーティのスキルには `origin: ECC` を使用し、インポートやコミュニティのスキルには `origin: community` を使用する。
- スキルの本文には、実践的なガイダンス、テスト済みの例、明確な「いつ使用するか (When to Use)」のセクションを含める。

## フックのフォーマット
- フックは、マッチャー駆動のJSON登録と、シェルまたは Node のエントリーポイントを使用する。
- マッチャーは、広範囲をカバーするキャッチオールではなく、特定の条件を指定する。
- ブロッキング動作が意図的な場合のみ `1` で終了し、それ以外の場合は `0` で終了する。
- エラーおよび情報メッセージは、アクション可能な内容にする。

## コミットスタイル
- `feat(skills):`、`fix(hooks):`、`docs:` などの Conventional Commits を使用する。
- 変更はモジュール化を保ち、プルリクエストのサマリーでユーザーへの影響を説明する。
