# ルール

## 必ず行うべきこと (Must Always)
- ドメインタスクは専門のエージェントに委譲する。
- 実装前にテストを記述し、クリティカルパスを検証する。
- 入力をバリデーションし、セキュリティチェックを維持する。
- 共有状態を変更するより、イミュータブルな更新を優先する。
- 新しいパターンを発明する前に、リポジトリで確立されたパターンに従う。
- コントリビューションは焦点を絞り、レビュー可能で、適切に説明された状態に保つ。

## 決して行ってはならないこと (Must Never)
- APIキー、トークン、シークレット、絶対パス/システムファイルパスなどの機密データを出力に含める。
- テストされていない変更を提出する。
- セキュリティチェックやバリデーションフックをバイパスする。
- 明確な理由なしに既存の機能を複製する。
- 関連するテストスイートを確認せずにコードを出荷する。

## エージェントのフォーマット
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含むYAMLフロントマターを含める。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させる。
- `description` は、そのエージェントがいつ呼び出されるべきかを明確に伝える。

## スキルのフォーマット
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を含むYAMLフロントマターを含める。
- ファーストパーティのスキルには `origin: ECC` を使用し、インポートされた/コミュニティのスキルには `origin: community` を使用する。
- スキルの本文には、実践的なガイダンス、テスト済みの例、明確な「いつ使用するか (When to Use)」のセクションを含める。

## フックのフォーマット
- フックは、マッチャー駆動のJSON登録と、シェルまたはNodeのエントリポイントを使用する。
- マッチャーは、広範なキャッチオールではなく、具体的にする。
- ブロッキング動作が意図的である場合にのみ `1` で終了し、それ以外の場合は `0` で終了する。
- エラーおよび情報メッセージは、実行可能（actionable）にする。

## コミットスタイル
- `feat(skills):`、`fix(hooks):`、`docs:` などのコンベンショナルコミット (Conventional Commits) を使用する。
- 変更はモジュール形式に保ち、プルリクエストの要約でユーザーへの影響を説明する。
