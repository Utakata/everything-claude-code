# ルール

## Must Always
- ドメインのタスクは専門のエージェントに委譲すること。
- 実装の前にテストを作成し、クリティカルパスを検証すること。
- 入力を検証し、セキュリティチェックをそのまま維持すること。
- 共有状態をミューテーションするよりも、イミュータブルな更新を優先すること。
- 新しいパターンを発明する前に、リポジトリで確立されたパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、適切に説明された状態を維持すること。

## Must Never
- APIキー、トークン、シークレット、絶対パス/システムファイルパスなどの機密データをの出力に含めないこと。
- テストされていない変更をサブミットしないこと。
- セキュリティチェックや検証フックをバイパスしないこと。
- 明確な理由なく既存の機能を複製しないこと。
- 関連するテストスイートをチェックせずにコードを出荷しないこと。

## Agent Format
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含むYAMLフロントマターを含める。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させること。
- Description はエージェントを呼び出すべきタイミングを明確に伝える必要がある。

## Skill Format
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を含むYAMLフロントマターを含める。
- ファーストパーティのスキルには `origin: ECC` を使用し、インポートされた/コミュニティのスキルには `origin: community` を使用する。
- スキルの本文には、実践的なガイダンス、テストされた例、および明確な「When to Use」セクションを含めるべきである。

## Hook Format
- フックは、マッチャー駆動のJSON登録とシェルまたはNodeエントリポイントを使用する。
- マッチャーは、広範囲をカバーするキャッチオールではなく、具体的であるべきである。
- ブロックする動作が意図的である場合にのみ `1` を終了する。それ以外の場合は `0` を終了する。
- エラーおよび情報メッセージは実用的なものであるべきである。

## Commit Style
- `feat(skills):`、`fix(hooks):`、`docs:` のようなConventional Commitsを使用すること。
- 変更はモジュール式に保ち、PRのサマリーでユーザーへの影響を説明すること。
