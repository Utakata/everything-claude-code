# ルール

## 必須事項
- ドメインタスクは専門のエージェントに委譲する。
- 実装前にテストを記述し、クリティカルパスを検証する。
- 入力を検証し、セキュリティチェックをそのまま維持する。
- 共有状態の変更よりもイミュータブルな更新を優先する。
- 新たなパターンを考案する前に、リポジトリで確立されているパターンに従う。
- コントリビューションは焦点を絞り、レビュー可能で、十分に説明された状態を保つ。

## 禁止事項
- APIキー、トークン、シークレット、絶対パスやシステムのファイルパスなどの機密データを出力に含める。
- 未テストの変更を提出する。
- セキュリティチェックやバリデーションフックをバイパスする。
- 明確な理由なく既存の機能を複製する。
- 関連するテストスイートを確認せずにコードをリリースする。

## Agentのフォーマット
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、`model` を含む YAML frontmatter を記述する。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させる。
- `description` には、エージェントがいつ呼び出されるべきかを明確に記載する。

## Skillのフォーマット
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、`origin` を含む YAML frontmatter を記述する。
- ファーストパーティのスキルには `origin: ECC` を、インポートされたスキルやコミュニティのスキルには `origin: community` を使用する。
- スキルの本文には、実践的なガイダンス、テスト済みの例、および明確な「When to Use（いつ使用すべきか）」セクションを含める。

## Hookのフォーマット
- フックは、マッチャー駆動の JSON 登録と、shell または Node のエントリーポイントを使用する。
- マッチャーは、広範なキャッチオールではなく、特定の条件にする。
- ブロッキング動作が意図的な場合のみ `1` で終了（exit `1`）し、それ以外の場合は `0` で終了（exit `0`）する。
- エラーメッセージや情報メッセージは、具体的な対応が可能（actionable）な内容にする。

## Commitのスタイル
- `feat(skills):`、`fix(hooks):`、`docs:` などの conventional commits を使用する。
- 変更はモジュール単位に保ち、ユーザーへの影響を PR summary で説明する。