# Rules

## Must Always
- ドメインタスクは特化型エージェントに委譲すること。
- 実装前にテストを記述し、クリティカルパスを検証すること。
- 入力を検証し、セキュリティチェックをそのまま維持すること。
- 共有状態のミューテーション（状態の変更）よりも、不変的な更新を優先すること。
- 新しいパターンを発明する前に、リポジトリの確立されたパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、適切に説明されている状態に保つこと。

## Must Never
- APIキー、トークン、シークレット、絶対パス/システムパスなどの機密データを出力に含めないこと。
- テストされていない変更を送信しないこと。
- セキュリティチェックや検証フックをバイパスしないこと。
- 明確な理由なしに既存の機能を重複させないこと。
- 関連するテストスイートを確認せずにコードを出荷しないこと。

## Agent Format
- エージェントは `agents/*.md` に配置する。
- 各ファイルには `name`, `description`, `tools`, および `model` を含む YAML フロントマターを含めること。
- ファイル名はハイフン区切りの小文字とし、エージェント名と一致させること。
- 説明（descriptions）は、エージェントがいつ呼び出されるべきかを明確に伝えること。

## Skill Format
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには `name`, `description`, および `origin` を含む YAML フロントマターを含めること。
- ファーストパーティのスキルには `origin: ECC` を使用し、インポート/コミュニティのスキルには `origin: community` を使用すること。
- スキルの本文には、実践的なガイダンス、テスト済みの例、明確な「使用するタイミング」のセクションを含めること。

## Hook Format
- フックはマッチャー駆動の JSON 登録と、シェルまたは Node エントリポイントを使用する。
- マッチャーは、広範囲のキャッチオールではなく、具体的であること。
- ブロック動作が意図的な場合のみ終了コード `1` を使用し、それ以外は `0` で終了すること。
- エラーおよび情報メッセージは、アクション可能であること。

## Commit Style
- `feat(skills):`, `fix(hooks):`, または `docs:` のような規約に従ったコミット（conventional commits）を使用すること。
- 変更はモジュール化し、PRのサマリーでユーザーへの影響を説明すること。