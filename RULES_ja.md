# Rules

## Must Always
- ドメインのタスクは特化したエージェントに委譲すること。
- 実装前にテストを記述し、クリティカルパスを検証すること。
- 入力を検証し、セキュリティチェックを維持すること。
- 共有状態を直接変更するのではなく、イミュータブル（不変）な更新を優先すること。
- 新しいパターンを発明する前に、リポジトリに確立されているパターンに従うこと。
- コントリビューションは焦点を絞り、レビュー可能で、適切に説明された状態を保つこと。

## Must Never
- APIキー、トークン、シークレット、絶対/システムファイルパスなどの機密データを、出力に含めないこと。
- テストされていない変更を提出しないこと。
- セキュリティチェックやバリデーションフックをバイパスしないこと。
- 明確な理由なく、既存の機能を重複させないこと。
- 関連するテストスイートをチェックせずにコードを出荷しないこと。

## Agent Format
- エージェントは `agents/*.md` に配置する。
- 各ファイルには、`name`、`description`、`tools`、および `model` を含む YAML フロントマターを含めること。
- ファイル名はハイフン区切りの小文字であり、エージェント名と一致しなければならない。
- `description` には、そのエージェントをいつ呼び出すべきかを明確に伝える必要がある。

## Skill Format
- スキルは `skills/<name>/SKILL.md` に配置する。
- 各スキルには、`name`、`description`、および `origin` を含む YAML フロントマターを含めること。
- ファーストパーティのスキルには `origin: ECC` を、インポート/コミュニティのスキルには `origin: community` を使用すること。
- スキルの本文には、実践的なガイダンス、テスト済みの例、そして明確な「When to Use」セクションを含める必要がある。

## Hook Format
- フックはマッチャー駆動型のJSON登録を使用し、シェルまたはNodeのエントリポイントを持つ。
- マッチャーは、広範囲をキャッチするものではなく、具体的なものであるべきだ。
- 意図的にブロックする場合のみ `1` で終了し、それ以外は `0` で終了すること。
- エラーおよび情報メッセージは、アクション可能である必要がある。

## Commit Style
- `feat(skills):`、`fix(hooks):`、または `docs:` のようなConventional Commitsを使用すること。
- 変更はモジュール化し、PRのサマリーでユーザーへの影響を説明すること。
