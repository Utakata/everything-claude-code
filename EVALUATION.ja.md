# リポジトリの評価と現在の設定

**日付:** 2026-03-21
**支店:** `claude/evaluate-repo-comparison-ASZ9Y`

---

## 現在のセットアップ (`~/.claude/`)

アクティブなクロード コードのインストールはほぼ最小限だ。

|コンポーネント |現在 |
|----------|----------|
|エージェント | 0 |
|スキル | 0 がインストールされています |
|コマンド | 0 |
|フック | 1 (停止: git check) |
|ルール | 0 |
| MCP 構成 | 0 |

**インストールされているフック:**
- `Stop` → `stop-hook-git-check.sh` — コミットされていない変更またはプッシュされていないコミットがある場合、セッションの終了をブロックする。

**インストールされている権限:**
- `Skill` — スキルの呼び出しを許可します

**プラグイン:** `blocklist.json` のみ (アクティブなプラグインはインストールされていません)

---

## このリポジトリ (`everything-claude-code` v1.9.0)

|コンポーネント |リポ |
|-----------|------|
|エージェント | 28 |
|スキル | 116 |
|コマンド | 59 |
|ルールセット | 12 言語 + 共通 (60 以上のルール ファイル) |
|フック |包括的なシステム (PreToolUse、PostToolUse、SessionStart、Stop) |
| MCP 構成 | 1 (Context7 + その他) |
|スキーマ | 9 JSON バリデータ |
|スクリプト/CLI | 46 個以上の Node.js モジュール + 複数の CLI |
|テスト | 58 テスト ファイル |
|プロファイルをインストールする |コア、開発者、セキュリティ、研究、フル |
|サポートされているハーネス |クロード コード、コーデックス、カーソル、OpenCode |

---

## ギャップ分析

### フック
- **現在:** 1 ストップフック (git 衛生チェック)
- **リポジトリ:** フック マトリックスを完全にカバー:
  - 危険なコマンドのブロック (`rm -rf`、強制プッシュ)
  - ファイル編集時の自動フォーマット
  - 開発サーバーの tmux の強制
  - コスト追跡
  - セッションの評価とガバナンスのキャプチャ
  - MCP ヘルスモニタリング

### エージェント (28 人が不足)
このリポジトリは、あらゆる主要なワークフローに特化したエージェントを提供する。
- 言語レビュー担当者: TypeScript、Python、Go、Java、Kotlin、Rust、C++、Flutter
- ビルドリゾルバー: Go、Java、Kotlin、Rust、C++、PyTorch
- ワークフロー エージェント: プランナー、tdd ガイド、コードレビュー担当者、セキュリティレビュー担当者、アーキテクト
- 自動化: ループ オペレーター、ドキュメント アップデーター、リファクター クリーナー、ハーネス オプティマイザー

### スキル (116 個がありません)
以下をカバーするドメイン知識モジュール:
- 言語パターン (Python、Go、Kotlin、Rust、C++、Java、Swift、Perl、Laravel、Django)
- テスト戦略 (TDD、E2E、カバレッジ)
- アーキテクチャ パターン (バックエンド、フロントエンド、API 設計、データベース移行)
- AI/ML ワークフロー (Claude API、評価ハーネス、エージェント ループ、コストを意識したパイプライン)
- ビジネスワークフロー (投資家向け資料、市場調査、コンテンツエンジン)

### コマンド (59 個が欠落しています)
- `/tdd`、`/plan`、`/e2e`、`/code-review` — コア開発ワークフロー
- `/sessions`、`/save-session`、`/resume-session` — セッションの永続性
- `/orchestrate`、`/multi-plan`、`/multi-execute` — マルチエージェントの調整
- `/learn`、`/skill-create`、`/evolve` — 継続的な改善
- `/build-fix`、`/verify`、`/quality-gate` — ビルド/品質自動化

### ルール (60 以上のファイルがありません)
言語固有のコーディング スタイル、パターン、テスト、およびセキュリティ ガイドライン:
TypeScript、Python、Go、Java、Kotlin、Rust、C++、C#、Swift、Perl、PHP、および共通/言語間ルール。

---

## 推奨事項

### 即値 (コアのインストール)
`ecc install --profile core` を実行して以下を取得する。
- コアエージェント (コードレビュー担当者、プランナー、TDD ガイド、セキュリティレビュー担当者)
- 必須スキル (tdd ワークフロー、コーディング標準、セキュリティ レビュー)
- 主要なコマンド (/tdd、/plan、/code-review、/build-fix)

### フルインストール
`ecc install --profile full` を実行して、28 人のエージェント、116 のスキル、および 59 のコマンドをすべて取得する。

### フックのアップグレード
現在のストップフックはしっかりしている。リポジトリの `hooks.json` には次が追加される。
- 危険なコマンドのブロック（安全性）
- 自動フォーマット（品質）
- コスト追跡（可観測性）
・セッション評価（学習）

### ルール
言語ルール (TypeScript、Python など) を追加すると、セッションごとのプロンプトに依存せずに、常に有効なコーディング ガイドラインが提供される。

---

## 現在の設定でうまくいくこと

- `stop-hook-git-check.sh` 停止フックは実稼働品質であり、すでに良好な Git 衛生状態を実現しています
- `Skill` 権限が正しく構成されている
- セットアップはクリーンで、競合や粗雑なものはありません

---

## 概要

現在のセットアップは、基本的には、適切に実装された git Hygiene フックが 1 つある白紙の状態だ。このリポジトリは、エージェント、スキル、コマンド、フック、ルールをカバーする、実稼働環境でテストされた完全な拡張レイヤーを提供する。また、選択的インストール システムを備えているため、構成を肥大化させることなく、必要なものを正確に追加できる。